config:
  platform: linux
  image_resource:
    type: docker-image
    source:
      repository: golang
      tag: latest
  inputs:
    - name: build
  run:
    path: sh
    args:
      - -c
      - |
        echo "Running tests..."
        cd build
        pwd
        ls -lah
        go test -v ./...
